{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "gcc - Build and debug active file",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/cub3d",
            "args": ["./maps/mapPrueba.cub"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++"
        }
    ]
}