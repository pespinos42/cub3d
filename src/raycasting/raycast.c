#include "cub3d.h"

/* void	position_direcction_ray(t_ray *r)
{
	
	//Calcula la posicion y direccion del rayo
	r->camera_x = 2 * x / (float)number_colums(r->m) - 1;
	r->ray_dir_x = r->p->dir_x + r->p->plane_x * r->camera_x;
	r->ray_dir_y = r->p->dir_y + r->p->plane_y * r->camera_x;
} */